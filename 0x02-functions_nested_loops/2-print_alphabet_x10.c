#include "main.h"
/**
 *Print alphabet - void print alphabets 10x
 *
 *Return: Always 0 (Success)
 */
void print_alphabet_x10(void)
{
_putchar n, so;
for (n = 'a'; n <= 'z'; n++)
{_putcharn(n);
}
so = 0
while (so < 10)
so++;
_putchar('\n');
}

