#include <stdio.h>
#include <string.h>
#include "main.h"

/**
*rev_string - function that reverses a string
*Description: parameterstring s
*Return: Always 0
*/

void rev_string(char *s)
{
char *s(100);
putchar("my name\n")
gets(s);
strrev(s);
putchar("ym eman: %s\n", *s);
return (0);
}

